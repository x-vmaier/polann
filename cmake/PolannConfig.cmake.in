include(CMakeFindDependencyMacro)

# Add any dependencies your library needs
# find_dependency(SomePackage REQUIRED)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/PolannTargets.cmake")

# Provide some information about the package
set(POLANN_VERSION @PROJECT_VERSION@)

check_required_components(polann)
